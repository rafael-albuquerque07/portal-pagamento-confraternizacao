/**
 * Service worker mínimo — cache básico do shell do app.
 * TODO: definir estratégia de cache (assets estáticos) quando o build estiver definido.
 */
self.addEventListener("install", () => {
  // placeholder
});

self.addEventListener("activate", () => {
  // placeholder
});

self.addEventListener("fetch", () => {
  // placeholder — passthrough (sem interceptação por enquanto)
});
