/**
 * Painel administrativo — visão geral de participantes x parcelas.
 * TODO: consumir GET /api/admin/dashboard (autenticado).
 */
export function renderPaginaAdmin(container: HTMLElement): void {
  container.innerHTML = `
    <main>
      <h1>Painel administrativo</h1>
      <p>Em construção.</p>
    </main>
  `;
}
