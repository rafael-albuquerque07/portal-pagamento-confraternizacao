/**
 * Componente de exibição do QR Code Pix + botão "Copiar código".
 * TODO: receber base64 do QR e o payload copia-e-cola da API.
 */
export function renderQrCodePix(
  container: HTMLElement,
  _qrCodeBase64: string,
  _payloadCopiaECola: string,
): void {
  container.innerHTML = `<div class="qrcode-pix">Em construção.</div>`;
}
