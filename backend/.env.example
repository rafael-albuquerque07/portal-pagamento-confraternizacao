ASAAS_API_KEY=
ASAAS_ENV=sandbox
ASAAS_WEBHOOK_TOKEN=
WHATSAPP_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_ADMIN_NUMBER=
# Hex digest SHA-256 da senha do admin (não a senha em texto puro). Gerar com:
# python -c "import hashlib; print(hashlib.sha256(b'sua-senha').hexdigest())"
ADMIN_PASSWORD_HASH=
DATABASE_URL=sqlite:///./confraternizacao.db
